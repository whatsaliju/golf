{
  "name": "golf-hole-flyover",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Real-data MapLibre golf hole flyover (OSM geometry + USGS 3DEP terrain + USGS/NAIP imagery)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "bake": "node scripts/bake-hole.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1"
  }
}
